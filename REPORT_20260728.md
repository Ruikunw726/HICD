# 工作日报 - 2026-07-28

## 今日完成

### 1. WSL2 环境搭建
- 在 Windows 10 上安装 WSL2 (Ubuntu 26.04)
- 解决 VPN 导致的 DNS 劫持问题，配置正确的 nameserver
- 安装 Miniconda + Python 3.11 环境

### 2. MambaCD 代码修复（6 个关键 bug）
- **`_read_tif`**: GDAL 返回 (C,H,W) 格式 → 统一转为 (H,W,C)
- **`_random_augment`**: rot90 axes=(1,2) → axes=(0,1) 适配 HWC 格式
- **CLIPTextEncoder**: embed_dim 512 → 128 匹配视觉特征维度
- **CrossAttentionFusion**: text_dim 512 → 128
- **cfg 传递**: yacs config → 手动构建 cfg_dict 映射
- **instances.json**: key 格式兼容 + img_stem 去除 _target 后缀

### 3. 全量训练脚本
- 编写 `train_full.py`: 支持 Airports + Ports + Urban-Rural 三场景联合训练
- 自动检测扁平/标准目录结构
- Windows 路径自动转换为 WSL 路径
- 支持混合精度训练、梯度裁剪、断点续训

### 4. Docker 部署准备
- 编写 Dockerfile (基于 nvidia/cuda:12.4.1)
- 生成 requirements.txt
- 测试数据集创建 (5 样本快速验证)

### 5. 代码提交
- 推送到 GitHub: https://github.com/Ruikunw726/mambacd.git
- 新增文件: train_full.py, Dockerfile, TROUBLESHOOTING.md
- 提交记录: e4f09ef, d0e0cc1

---

## 踩坑总结

| 类别 | 问题数 | 主要问题 |
|------|--------|---------|
| WSL/网络 | 2 | DNS 劫持、路径格式 |
| Python 环境 | 3 | 版本不兼容、编译依赖、conda TOS |
| PyTorch/CUDA | 2 | CUDA 版本不匹配、扩展编译 |
| 数据维度 | 3 | GDAL 格式、augment axes、CLIP 维度 |
| 模型初始化 | 2 | cfg 传递、权重格式 |
| 数据集结构 | 2 | 目录结构、key 格式 |

详见: [TROUBLESHOOTING.md](./TROUBLESHOOTING.md)

---

## 当前状态

- ✅ 本地训练已跑通 (TITAN RTX, ~30 min/epoch)
- ⏳ 全量训练进行中 (100 epochs, 预计 50 小时)
- 📦 Docker 镜像待构建 (需安装 Docker)

---

## 明日计划

1. 监控全量训练进度，观察 loss 收敛情况
2. 构建 Docker 镜像，推送到服务器
3. 服务器 A100 训练 (预计 6-8 小时完成)
4. 评估模型性能，调整超参数

---

## 时间线

| 时间 | 事项 |
|------|------|
| 09:00 | 安装 WSL2，配置网络 |
| 10:30 | 搭建 Python 环境，安装依赖 |
| 12:00 | 修复 _read_tif、_random_augment 维度问题 |
| 14:00 | 修复 CLIP 维度、cfg 传递问题 |
| 15:30 | 测试数据集验证通过 |
| 16:00 | 编写 train_full.py 全量训练脚本 |
| 17:00 | 编写 Dockerfile，提交代码 |
| 18:00 | 全量训练启动 |

---

## 技术债务

1. `_read_tif` 只取前 3 通道，丢失第 4 通道信息 (如有用需处理)
2. 没有 VSSM 预训练权重，当前是随机初始化
3. 验证集评估只计算 loss，没有计算 mAP 等检测指标
4. 数据增强较简单，可增加 color jitter、mixup 等
